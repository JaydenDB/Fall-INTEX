<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Ella Rises – Add Event Template</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@400;600;700&family=Playfair+Display:wght@500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="page">
    <%- include('partials/header') %>

    <section class="section">
      <div class="container" style="max-width:700px;">
        <div class="section-header" style="text-align:left;">
          <p class="section-kicker">Events</p>
          <h2 class="section-title">Add new event template</h2>
          <p class="section-subtitle" style="margin-left:0;">
            Define a reusable event template that can be used to schedule occurrences and track registrations and outcomes.
          </p>
        </div>

        <div class="card">
          <% if (error) { %>
            <div class="alert-error">
              <%= error %>
            </div>
          <% } %>

          <form
            method="POST"
            action="/events/new"
            style="display:flex; flex-direction:column; gap:0.7rem;"
          >
            <div>
              <label style="font-size:0.8rem;">Event name</label>
              <input
                type="text"
                name="eventName"
                required
                value="<%= formData && formData.eventName ? formData.eventName : '' %>"
                style="width:100%; padding:0.5rem 0.7rem; border-radius:10px; border:1px solid var(--border-soft); font-size:0.9rem;"
              />
            </div>

            <div style="display:grid; grid-template-columns:1fr 1fr; gap:0.6rem;">
              <div>
                <label style="font-size:0.8rem;">Event type</label>
                <input
                  type="text"
                  name="eventType"
                  placeholder="Workshop, Summit, Club, etc."
                  value="<%= formData && formData.eventType ? formData.eventType : '' %>"
                  style="width:100%; padding:0.5rem 0.7rem; border-radius:10px; border:1px solid var(--border-soft); font-size:0.9rem;"
                />
              </div>
              <div>
                <label style="font-size:0.8rem;">Default capacity</label>
                <input
                  type="number"
                  min="0"
                  name="defaultCapacity"
                  value="<%= formData && formData.defaultCapacity ? formData.defaultCapacity : '' %>"
                  style="width:100%; padding:0.5rem 0.7rem; border-radius:10px; border:1px solid var(--border-soft); font-size:0.9rem;"
                />
              </div>
            </div>

            <div>
              <label style="font-size:0.8rem;">Recurrence pattern (optional)</label>
              <input
                type="text"
                name="recurrencePattern"
                placeholder="e.g., Monthly on Saturdays"
                value="<%= formData && formData.recurrencePattern ? formData.recurrencePattern : '' %>"
                style="width:100%; padding:0.5rem 0.7rem; border-radius:10px; border:1px solid var(--border-soft); font-size:0.9rem;"
              />
            </div>

            <div>
              <label style="font-size:0.8rem;">Description</label>
              <textarea
                name="eventDescription"
                rows="4"
                style="width:100%; padding:0.6rem 0.7rem; border-radius:10px; border:1px solid var(--border-soft); font-size:0.9rem; resize:vertical;"
              ><%= formData && formData.eventDescription ? formData.eventDescription : '' %></textarea>
            </div>

            <div style="margin-top:0.7rem; display:flex; gap:0.7rem;">
              <button type="submit" class="btn btn-primary">
                Save event template
              </button>
              <a href="/events" class="btn btn-ghost">Cancel</a>
            </div>
          </form>
        </div>
      </div>
    </section>

    <%- include('partials/footer') %>
  </div>
</body>
</html>
