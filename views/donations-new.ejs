<!DOCTYPE html>
<html lang="en">
<head>
  <!-- basic page setup -->
  <meta charset="UTF-8" />
  <title>Ella Rises – Donate</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- fonts + main stylesheet -->
  <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@400;600;700&family=Playfair+Display:wght@500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="page">
    <!-- shared header navigation -->
    <%- include('partials/header') %>

    <section class="section">
      <div class="container" style="max-width:640px;">

        <!-- page header / intro -->
        <div class="section-header">
          <img src="/images/ella%20rises%20logo.png" alt="Ella Rises Logo"
               style="width: 80px; height: 80px; margin: 0 auto 1rem; display: block;">
          <p class="section-kicker">Donations</p>
          <h2 class="section-title">Support Ella Rises</h2>
          <p class="section-subtitle">
            Your donation funds workshops, mentoring, instruments, and STEAM experiences.
          </p>
        </div>

        <div class="card">
          <!-- error + success alerts passed from server -->
          <% if (error) { %>
            <div class="alert-error">
              <%= error %>
            </div>
          <% } %>

          <% if (success) { %>
            <div class="alert-success">
              <%= success %>
            </div>
          <% } %>

          <!-- donation form (POST back to /donate) -->
          <form method="POST" action="/donate" style="display:flex; flex-direction:column; gap:0.8rem;">

            <!-- receipt email -->
            <div>
              <label style="font-size:0.8rem;">Email (for receipt)</label>
              <input name="email" type="email"
                style="width:100%; padding:0.5rem 0.7rem; border-radius:10px; border:1px solid var(--border-soft); font-size:0.9rem;" />
            </div>

            <!-- required donation amount -->
            <div>
              <label style="font-size:0.8rem;">Donation amount (USD)</label>
              <input name="amount" type="number" step="0.01" min="1" required
                style="width:100%; padding:0.5rem 0.7rem; border-radius:10px; border:1px solid var(--border-soft); font-size:0.9rem;" />
            </div>

            <!-- fake payment section (demo-only) -->
            <div style="border-top:1px solid rgba(235,216,255,0.9); padding-top:0.7rem; margin-top:0.3rem;">
              <p style="font-size:0.8rem; color:var(--text-muted); margin-bottom:0.4rem;">
                Payment information (demo only – not processed).
              </p>

              <!-- cardholder name -->
              <div>
                <label style="font-size:0.8rem;">Name on card</label>
                <input name="nameOnCard" type="text"
                  style="width:100%; padding:0.5rem 0.7rem; border-radius:10px; border:1px solid var(--border-soft); font-size:0.9rem;" />
              </div>

              <!-- card number (stored nowhere) -->
              <div>
                <label style="font-size:0.8rem;">Card number</label>
                <input name="cardNumber" type="text" maxlength="19"
                  style="width:100%; padding:0.5rem 0.7rem; border-radius:10px; border:1px solid var(--border-soft); font-size:0.9rem;" />
              </div>

              <!-- expiration + CVV inputs -->
              <div style="display:grid; grid-template-columns:1fr 1fr 1fr; gap:0.6rem;">
                <div>
                  <label style="font-size:0.8rem;">Exp. month</label>
                  <input name="expMonth" type="text" placeholder="MM"
                    style="width:100%; padding:0.5rem 0.7rem; border-radius:10px; border:1px solid var(--border-soft); font-size:0.9rem;" />
                </div>
                <div>
                  <label style="font-size:0.8rem;">Exp. year</label>
                  <input name="expYear" type="text" placeholder="YYYY"
                    style="width:100%; padding:0.5rem 0.7rem; border-radius:10px; border:1px solid var(--border-soft); font-size:0.9rem;" />
                </div>
                <div>
                  <label style="font-size:0.8rem;">CVV</label>
                  <input name="cvv" type="text" maxlength="4"
                    style="width:100%; padding:0.5rem 0.7rem; border-radius:10px; border:1px solid var(--border-soft); font-size:0.9rem;" />
                </div>
              </div>

              <!-- simple explanation because this is not real payment processing -->
              <p style="font-size:0.75rem; color:var(--text-muted); margin-top:0.5rem;">
                For this class project, card details are <strong>not</strong> stored or processed.
              </p>
            </div>

            <!-- submit button -->
            <button type="submit" class="btn btn-primary" style="margin-top:0.8rem;">
              Donate now
            </button>
          </form>
        </div>
      </div>
    </section>

    <!-- shared footer -->
    <%- include('partials/footer') %>
  </div>
</body>
</html>
