<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Ella Rises – Login</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@400;600;700&family=Playfair+Display:wght@500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="page">
    <%- include('partials/header') %>

    <section class="section">
      <div class="container" style="max-width: 480px;">
        <div class="section-header">
          <p class="section-kicker">Account</p>
          <h2 class="section-title">Log in to Ella Rises Portal</h2>
          <p class="section-subtitle">
            Admins can manage participants, events, surveys, milestones, and donations.
            Participants can view their progress and upcoming events.
          </p>
        </div>

        <div class="card">
          <% if (error) { %>
            <div style="
              background: #ffe5e8;
              border-radius: 12px;
              padding: 0.6rem 0.8rem;
              font-size: 0.82rem;
              color: #8a102b;
              margin-bottom: 0.9rem;
              border: 1px solid #ffc3cd;
            ">
              <%= error %>
            </div>
          <% } %>

          <form method="POST" action="/login" style="display: flex; flex-direction: column; gap: 0.9rem;">
            <div>
              <label for="email" style="display:block; font-size:0.85rem; margin-bottom:0.25rem;">Email</label>
              <input
                type="email"
                id="email"
                name="email"
                required
                style="
                  width: 100%;
                  padding: 0.55rem 0.7rem;
                  border-radius: 10px;
                  border: 1px solid var(--border-soft);
                  font-size: 0.9rem;
                "
              />
            </div>

            <div>
              <label for="password" style="display:block; font-size:0.85rem; margin-bottom:0.25rem;">Password</label>
              <input
                type="password"
                id="password"
                name="password"
                required
                style="
                  width: 100%;
                  padding: 0.55rem 0.7rem;
                  border-radius: 10px;
                  border: 1px solid var(--border-soft);
                  font-size: 0.9rem;
                "
              />
            </div>

            <button type="submit" class="btn btn-primary" style="margin-top:0.5rem; width:100%; justify-content:center;">
              Log in
            </button>

            <p style="font-size:0.8rem; color:var(--text-muted); margin-top:0.6rem;">
              If you don’t have an account yet, please contact an Ella Rises administrator.
            </p>
          </form>
        </div>
      </div>
    </section>

    <%- include('partials/footer') %>
  </div>
</body>
</html>
