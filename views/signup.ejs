<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Ella Rises – Sign up</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Shared fonts + main site stylesheet -->
  <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@400;600;700&family=Playfair+Display:wght@500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="page">
    <!-- Global header (logo + nav + login state) -->
    <%- include('partials/header') %>

    <section class="section">
      <div class="container" style="max-width: 520px;">
        <!-- Page heading for the signup flow -->
        <div class="section-header">
          <img
            src="/images/ella%20rises%20logo.png"
            alt="Ella Rises Logo"
            style="width: 80px; height: 80px; margin: 0 auto 1rem; display: block;"
          >
          <p class="section-kicker">Account</p>
          <h2 class="section-title">Create your Ella Rises account</h2>
          <p class="section-subtitle">
            Participants can create an account to see their information and be linked to surveys and milestones.
          </p>
        </div>

        <div class="card">
          <!-- Show an error message if the server passed one into the view -->
          <% if (error) { %>
            <div class="alert-error">
              <%= error %>
            </div>
          <% } %>

          <!-- Self-signup form -->
          <form
            method="POST"
            action="/signup"
            enctype="multipart/form-data"
            style="display: flex; flex-direction: column; gap: 0.9rem;"
          >
            <!-- Name row -->
            <div style="display:grid; grid-template-columns:1fr 1fr; gap:0.6rem;">
              <div>
                <label style="display:block; font-size:0.85rem; margin-bottom:0.25rem;">First name</label>
                <input
                  type="text"
                  name="firstName"
                  required
                  style="width:100%; padding:0.55rem 0.7rem; border-radius:10px; border:1px solid var(--border-soft); font-size:0.9rem;"
                />
              </div>
              <div>
                <label style="display:block; font-size:0.85rem; margin-bottom:0.25rem;">Last name</label>
                <input
                  type="text"
                  name="lastName"
                  required
                  style="width:100%; padding:0.55rem 0.7rem; border-radius:10px; border:1px solid var(--border-soft); font-size:0.9rem;"
                />
              </div>
            </div>

            <!-- Email used as login + contact -->
            <div>
              <label style="display:block; font-size:0.85rem; margin-bottom:0.25rem;">Email</label>
              <input
                type="email"
                name="email"
                required
                style="width:100%; padding:0.55rem 0.7rem; border-radius:10px; border:1px solid var(--border-soft); font-size:0.9rem;"
              />
            </div>

            <!-- Password for logging into the portal -->
            <div>
              <label style="display:block; font-size:0.85rem; margin-bottom:0.25rem;">Password</label>
              <input
                type="password"
                name="password"
                required
                placeholder="Create a password for login"
                style="width:100%; padding:0.55rem 0.7rem; border-radius:10px; border:1px solid var(--border-soft); font-size:0.9rem;"
              />
              <p style="font-size:0.8rem; color:var(--text-muted); margin-top:0.25rem;">
                This password will be used to log into your Ella Rises portal.
              </p>
            </div>

            <!-- Optional profile photo used for avatar circles around the site -->
            <div>
              <label style="display:block; font-size:0.85rem; margin-bottom:0.25rem;">Profile photo (optional)</label>
              <input
                type="file"
                name="photo"
                accept="image/*"
                style="width:100%; padding:0.4rem 0; font-size:0.85rem;"
              />
              <p style="font-size:0.8rem; color:var(--text-muted); margin-top:0.2rem;">
                Square images (e.g., 400×400) look best in the portal.
              </p>
            </div>

            <!-- Basic location + school data to help with impact stories -->
            <div style="display:grid; grid-template-columns:1fr 1fr; gap:0.6rem;">
              <div>
                <label style="display:block; font-size:0.85rem; margin-bottom:0.25rem;">City</label>
                <input
                  type="text"
                  name="city"
                  style="width:100%; padding:0.55rem 0.7rem; border-radius:10px; border:1px solid var(--border-soft); font-size:0.9rem;"
                />
              </div>
              <div>
                <label style="display:block; font-size:0.85rem; margin-bottom:0.25rem;">State</label>
                <input
                  type="text"
                  name="state"
                  style="width:100%; padding:0.55rem 0.7rem; border-radius:10px; border:1px solid var(--border-soft); font-size:0.9rem;"
                />
              </div>
            </div>

            <div>
              <label style="display:block; font-size:0.85rem; margin-bottom:0.25rem;">School (optional)</label>
              <input
                type="text"
                name="school"
                style="width:100%; padding:0.55rem 0.7rem; border-radius:10px; border:1px solid var(--border-soft); font-size:0.9rem;"
              />
            </div>

            <div>
              <label style="display:block; font-size:0.85rem; margin-bottom:0.25rem;">Field of interest (optional)</label>
              <input
                type="text"
                name="fieldOfInterest"
                style="width:100%; padding:0.55rem 0.7rem; border-radius:10px; border:1px solid var(--border-soft); font-size:0.9rem;"
              />
            </div>

            <!-- Primary CTA to create the account -->
            <button
              type="submit"
              class="btn btn-primary"
              style="margin-top:0.5rem; width:100%; justify-content:center;"
            >
              Create account
            </button>

            <!-- Link back to login for users who already exist -->
            <p style="font-size:0.8rem; color:var(--text-muted); margin-top:0.6rem;">
              Already have an account? <a href="/login">Log in</a>.
            </p>
          </form>
        </div>
      </div>
    </section>

    <!-- Shared footer -->
    <%- include('partials/footer') %>
  </div>
</body>
</html>
